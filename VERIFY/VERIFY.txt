ORL-AI v5.0.0 Verification

Run from the repository root:

python -B VERIFY_ALL.py

Windows:

VERIFY_ALL.bat

Linux or macOS:

sh verify_all.sh

The functional sequence checks:

- Python reference behavior;
- independent Python reconstruction;
- strict canonical corpus bytes;
- standalone JavaScript strict-JSON intake;
- JavaScript frozen-corpus parity;
- origin-independent pure-JavaScript SHA-256 fallback;
- raw-intake parity across Python and JavaScript;
- Python-JavaScript example and edge-case byte equality;
- committed artifact reproduction and second-run idempotence;
- seeded structural properties;
- decision-admission capsules;
- frozen state precedence;
- hostile inputs and falsification artifacts;
- direct bundle and capsule tamper rejection;
- artifact-profile identity binding;
- public-receipt identifier omission;
- structural identity separation;
- resource and text-profile controls;
- bounded relay-heavy witness construction across Python and JavaScript.

Expected committed counts:

Reference self-test: 26/26
Independent corpus: 10/10
Strict JavaScript parser: 19/19
JavaScript frozen parity: 10/10
SHA-256 fallback verification: 18/18
Raw-intake parity: 11/11
Live example cross-check: 10/10
Cross-language edge parity: 8/8
Deterministic reproduction: 10/10
Seeded properties: 64/64
Hostile corpus: 20/20
Falsification corpus: 11/11
Capsule self-test: 6/6
State precedence: 15/15

Hashing is a separate final-freeze activity and is not part of functional verification. When `hashes/SHA256SUMS.txt` is present, the GitHub Actions workflow verifies it.

A passing result applies only to the declared package, schema, profile, ruleset, text profile, artifact profile, examples, corpora, canonicalization, and verification procedures. It does not establish factual truth, source authenticity, actual model independence, formal verification, security qualification, domain safety, authorization, production qualification, or third-party certification.
